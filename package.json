{
  "scripts": {
    "build:frontend": "cd frontend && npm install && npm run build"
  },
  "devDependencies": {
    "@types/react": "^19.1.7",
    "@types/react-dom": "^19.1.6"
  },
  "proxy": "http://localhost:8080"
}
